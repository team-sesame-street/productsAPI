// const pool = require('./db');

// const getProducts = (page = 1, count = 5) => {
//   return pool.query(`SELECT * from PRODUCTS limit $1`, [page * count])
// }

// const getProductInfo = (id) => {
//   return pool.query(`SELECT * from PRODUCTS limit $1`, [page * count])
// }

// const getProductStyles = (id) => {
//   return pool.query(`SELECT * from PRODUCTS limit $1`, [page * count])
// }

// module.exports = {
//   getProducts,
//   getProductInfo,
//   getProductStyles
// }